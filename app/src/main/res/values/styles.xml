<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="RxButton" parent="Base.TextAppearance.AppCompat.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Switch">
        <item name="android:switchMinWidth">66dp</item>
        <item name="android:thumb">@drawable/ui_switch_thumb</item>
        <item name="android:track">@drawable/ui_switch_track</item>
        <item name="android:thumbTextPadding">21.33dp</item>
        <item name="android:duplicateParentState">false</item>
        <item name="android:textOn">""</item>
        <item name="android:textOff">""</item>
    </style>
</resources>